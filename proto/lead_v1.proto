syntax = "proto3";

package tracefabric;

message RawLead {
  string id = 1;
  string source_url = 2;
  string company_name = 3;
  string raw_html = 4;

  // should be epoch or ISO8601 string
  string timestamp = 5;
  
}

message LeadBatch {
  repeated RawLead leads = 1;
}